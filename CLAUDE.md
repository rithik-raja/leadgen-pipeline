We are making a set of Python scripts to generate leads using AI. Our goal is to find local businesses and establishments in specific sectors and sub-sectors and target them with hyper-optimized emails.
I am a software development agency. My goal is vertical, outcome-specific positioning - rather than soliciting them as a general dev agency (they don't know what they want). I want to do a deep analysis of their online presence and offer "outcomes."

After every change, run `pyright --pythonpath venv/bin/python . 2>&1` and fix the errors that occur.